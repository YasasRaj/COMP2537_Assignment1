<%- include('templates/header', { styleSheets: [] }) %>

<div class="row justify-content-center align-items-center" style="min-height: 75vh;">
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card shadow-sm border-0">
            <div class="card-body p-5">
                <h2 class="text-center text-primary fw-bold mb-4">Create User</h2>
                
                <form action="/signup" method="post">
                    <!-- Name Input -->
                    <div class="mb-3">
                        <label for="name" class="form-label text-muted small fw-bold">Full Name</label>
                        <input name="name" type="text" class="form-control form-control-lg" id="name" placeholder="John Doe" required>
                    </div>

                    <!-- Email Input -->
                    <div class="mb-3">
                        <label for="email" class="form-label text-muted small fw-bold">Email Address</label>
                        <input name="email" type="email" class="form-control form-control-lg" id="email" placeholder="email@example.com" required>
                    </div>

                    <!-- Password Input -->
                    <div class="mb-4">
                        <label for="password" class="form-label text-muted small fw-bold">Password</label>
                        <input name="password" type="password" class="form-control form-control-lg" id="password" placeholder="••••••••" required>
                    </div>

                    <!-- Submit Button -->
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">Submit</button>
                    </div>
                </form>

                <!-- Error Handling -->
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger mt-4 py-2 small text-center" role="alert">
                        <%= error %>
                    </div>
                <% } %>
            </div>
        </div>
        
        <!-- Navigation back to Login -->
        <div class="text-center mt-3">
            <p class="text-muted small">Already have an account? <a href="/login" class="text-primary text-decoration-none">Log In</a></p>
        </div>
    </div>
</div>

<%- include('templates/footer', { scripts: [] }) %>